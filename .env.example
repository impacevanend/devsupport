# Archivo de ejemplo de configuración (.env)
# Copia este archivo y renómbralo a .env antes de ejecutar el proyecto

# --- App ---
NODE_ENV=development
PORT=3000

# --- Base de datos (elige UNA de las dos) ---

# Si ejecutas la API dentro de Docker Compose (service name: db)
DATABASE_URL="postgresql://app:app@db:5432/tickets?schema=public"

# Si ejecutas la API directamente en tu máquina (sin contenedor para la API)
# DATABASE_URL="postgresql://app:app@localhost:5432/tickets?schema=public"

# --- Opcionales útiles ---
# LOG_LEVEL=info
# CORS_ORIGIN=*
